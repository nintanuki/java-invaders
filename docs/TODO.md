# TODO

Quick scratch list. The ordered plan lives in [`ROADMAP.md`](ROADMAP.md);
this file is for "remember to do this small thing" notes.

- [ ] Verify `mvn compile exec:java` actually opens a window on this
      machine (Java version, Maven version, etc.).
- [ ] Decide whether to keep 2-space indentation or switch to 4-space — the
      copilot instructions say 2, which is fine, just be consistent.
- [ ] Once Milestone 1 starts: write a `Resources.loadImage(String path)`
      helper so every sprite doesn't repeat the `getResource` boilerplate.
